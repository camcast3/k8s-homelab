machine:
  network:
    hostname: {{ node_name }}
    interfaces:
      - interface: eth0
        addresses:
          - {{ node_ip }}/24
        routes:
          - network: 0.0.0.0/0
            gateway: {{ gateway }}
    nameservers:
{% for ns in nameservers %}
      - {{ ns }}
{% endfor %}
